package pang.backend.character.enemy;

import pang.backend.config.GameConfig;

public class MegaBall extends Enemy {
    MegaBall(GameConfig config, int spawnTime){
        super(config, spawnTime);
    }
}
